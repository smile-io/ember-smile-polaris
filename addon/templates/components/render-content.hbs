{{#if this.contentIsComponentHash}}
  {{component-proxy
    componentName=this.content.componentName
    props=this.content.props
  }}
{{else if this.contentIsComponentDefinition}}
  {{component this.content}}
{{else}}
  {{content}}
{{/if}}